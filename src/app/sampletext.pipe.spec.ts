import { SampletextPipe } from './sampletext.pipe';

describe('SampletextPipe', () => {
  it('create an instance', () => {
    const pipe = new SampletextPipe();
    expect(pipe).toBeTruthy();
  });
});
